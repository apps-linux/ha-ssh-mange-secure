name: "SSH Docker Manager"
version: "0.1.0"
slug: "ssh_docker_manager"
description: "Securely manage Docker containers on a remote Linux host over SSH, exposed to Home Assistant via MQTT."
url: "https://github.com/apps-linux/ha-ssh-mange-secure"
arch:
  - aarch64
  - amd64
  - armv7
init: false
startup: services
boot: auto
services:
  - "mqtt:want"
options:
  ssh:
    host: ""
    port: 22
    username: ""
    key_mode: "generate"
    private_key: ""
    private_key_passphrase: ""
  mqtt:
    discovery_prefix: "homeassistant"
    use_addon_broker: true
    host: ""
    port: 1883
    username: ""
    password: ""
  poll_interval: 30
schema:
  ssh:
    host: "str"
    port: "port"
    username: "str"
    key_mode: "list(generate|existing)"
    private_key: "str?"
    private_key_passphrase: "password?"
  mqtt:
    discovery_prefix: "str"
    use_addon_broker: "bool"
    host: "str?"
    port: "port?"
    username: "str?"
    password: "password?"
  poll_interval: "int(5,300)"
